# Copyright 2010 The ChromiumOS Authors
# Use of this source code is governed by a BSD-style license that can be
# found in the LICENSE file.

AUTHOR = "ChromeOS Team"
NAME = "logging_UserCrash"
METADATA = {
  'contacts': ['cros-telemetry@google.com'],
   # ChromeOS > Data > Engineering > Crash Reporting
  'bug_component': 'b:1032705',
  'criteria': 'Verify crash reporter initialization and metadata production',
}
PURPOSE = "Verify user space crashes are handled appropriately."
ATTRIBUTES = "suite:bvt-cq, suite:smoke, suite:satlab-qual-bvt-cq, suite:distributed_lab_qual_bvt_shard1"
TIME = "SHORT"
TEST_CATEGORY = "Functional"
TEST_CLASS = "logging"
TEST_TYPE = "client"
JOB_RETRIES = 2  # Per CheckRetry check this is recommended
# Reboot in the lab after the test ends. Since we lock the core_pattern
# at the end of the test, we should reboot to restore the state of the
# device.
DEPENDENCIES='cleanup-reboot'
PY_VERSION = 3

DOC = """
Test that user space crashes are detected and handled appropriately.
"""

job.run_test('logging_UserCrash')
