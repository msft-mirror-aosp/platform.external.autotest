# Copyright 2022 The ChromiumOS Authors
# Use of this source code is governed by a BSD-style license that can be
# found in the LICENSE file.

from autotest_lib.client.common_lib import utils

AUTHOR = 'The ChromiumOS Authors'
NAME = 'tast.shimlessrma_nodelocked_perbuild'
TIME = 'SHORT'
TEST_TYPE = 'Server'
DEPENDENCIES = 'servo_state:WORKING'
ATTRIBUTES = 'suite:shimlessrma_nodelocked_perbuild'
MAX_RESULT_SIZE_KB = 1024 * 1024

DOC = '''
Run the Tast Shimless RMA with node-locked image test suite.

Tast is an integration-testing framework analagous to the test-running portion
of Autotest. See https://chromium.googlesource.com/chromiumos/platform/tast/ for
more information.

See http://go/tast-failures for information about investigating failures.
'''

args_dict = utils.args_to_dict(args)
assert 'servo_state:WORKING' in DEPENDENCIES
servo_args = hosts.CrosHost.get_servo_arguments(args_dict)

def run(machine):
    job.run_test('tast',
                 host=hosts.create_host(machine, servo_args=servo_args),
                 test_exprs=['("group:shimless_rma" && shimless_rma_nodelocked)'],
                 ignore_test_failures=True,
                 max_run_sec=1800,  # 30 Minutes
                 command_args=args)

parallel_simple(run, machines)
