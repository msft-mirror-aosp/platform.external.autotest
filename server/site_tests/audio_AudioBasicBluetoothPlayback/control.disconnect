# Copyright 2015 The ChromiumOS Authors
# Use of this source code is governed by a BSD-style license that can be
# found in the LICENSE file.

from autotest_lib.server import utils

NAME = "audio_AudioBasicBluetoothPlayback.disconnect"
METADATA = {
    "contacts": ["chromeos-audio-sw@google.com"],
    "bug_component": "b:875558",
    "criteria": "This test will fail if the captured audio does not match original file.",
    "doc" : "Tests BT playback audio function after disconnect-connect"
}
TEST_TYPE = "server"
#ATTRIBUTES = "suite:audio_advanceds"
DEPENDENCIES = "chameleon, audio_board, test_bluetooth"
JOB_RETRIES = 1

args_dict = utils.args_to_dict(args)
chameleon_args = hosts.CrosHost.get_chameleon_arguments(args_dict)

def run(machine):
    host = hosts.create_host(machine, chameleon_args=chameleon_args)
    job.run_test("audio_AudioBasicBluetoothPlayback", host=host,
                 disconnect=True, tag="disconnect")

parallel_simple(run, machines)
