# Copyright 2022 The Chromium OS Authors. All rights reserved.
# Use of this source code is governed by a BSD-style license that can be
# found in the LICENSE file.

AUTHOR = 'Chromium OS team'
NAME = 'tast.shimlessrma_normal_perbuild'
TIME = 'SHORT'
TEST_TYPE = 'Server'
ATTRIBUTES = 'suite:shimlessrma_normal_perbuild'
MAX_RESULT_SIZE_KB = 1024 * 1024
PY_VERSION = 3

DOC = '''
Run the Tast Shimless RMA without node-locked image test suite.

Tast is an integration-testing framework analagous to the test-running portion
of Autotest. See https://chromium.googlesource.com/chromiumos/platform/tast/ for
more information.

See http://go/tast-failures for information about investigating failures.
'''

def run(machine):
    job.run_test('tast',
                 host=hosts.create_host(machine),
                 test_exprs=['("group:shimless_rma" && shimless_rma_normal)'],
                 ignore_test_failures=True,
                 max_run_sec=1800,  # 30 Minutes
                 command_args=args)

parallel_simple(run, machines)
